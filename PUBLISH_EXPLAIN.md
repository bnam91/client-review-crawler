# `--publish=always` 플래그 설명

## 역할

`--publish=always`는 `electron-builder`에게 **빌드 후 자동으로 GitHub Releases에 업로드**하라는 지시입니다.

## 차이점

### `npm run build:mac` (퍼블리시 없음)
```bash
electron-builder --mac
```
- ✅ 앱 빌드만 수행
- ❌ GitHub에 업로드 안 함
- 📁 빌드 파일은 `dist/` 폴더에만 저장
- 🔧 로컬 테스트용

### `npm run release:mac` (자동 퍼블리시)
```bash
electron-builder --mac --publish=always
```
- ✅ 앱 빌드 수행
- ✅ GitHub Releases에 자동 업로드
- ✅ 릴리즈 자동 생성/업데이트
- 🚀 배포용

## `--publish` 옵션 종류

| 옵션 | 설명 | 사용 시기 |
|------|------|----------|
| `--publish=never` | 퍼블리시 안 함 (기본값) | 로컬 빌드만 필요할 때 |
| `--publish=always` | 항상 퍼블리시 | 배포할 때 |
| `--publish=onTag` | Git 태그가 있을 때만 퍼블리시 | CI/CD 파이프라인에서 사용 |

## 예시

### 로컬에서 테스트만 하려면
```bash
npm run build:mac
# dist/ 폴더에 파일만 생성, GitHub에는 업로드 안 함
```

### GitHub에 자동 배포하려면
```bash
$env:GH_TOKEN="your_token"
npm run release:mac
# 빌드 + GitHub Releases에 자동 업로드
```

## 주의사항

- `--publish=always`를 사용하려면 `GH_TOKEN` 환경 변수가 필요합니다
- GitHub Releases에 자동으로 릴리즈가 생성/업데이트됩니다
- `package.json`의 `version`이 릴리즈 태그로 사용됩니다

